{
  "name": "@webtiers-ddd-poc/accounting-application",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/accounting/application/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "pnpm --filter @webtiers-ddd-poc/accounting-application run build"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["packages/accounting/application/src/**/*.ts"]
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @webtiers-ddd-poc/accounting-application run clean"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @webtiers-ddd-poc/accounting-application run typecheck"
      }
    }
  },
  "tags": []
}
